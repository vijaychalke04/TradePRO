{
  "name": "trade-genie",
  "version": "1.0.0",
  "description": "Trade Genie backend (login/fetch_all + Monte Carlo + Greeks + Telegram send-only)",
  "main": "server.js",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
